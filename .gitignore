# Dependencies
node_modules/

# Build output / packaged extension
dist/
*.zip

# Secrets — NEVER commit these
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Agent instructions — live ONLY in the private backend repo (docs/agent-guides/).
# Kept out of this public repo so strategy/roadmap isn't exposed.
CLAUDE.md
AGENTS.md

# Local tool/editor state
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/settings.local.json
extension/assets/*.png.bak
